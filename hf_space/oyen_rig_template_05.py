PART = '    tip = add_uv_sphere("Oyen_TailTip", tail_points[-1], (0.105, 0.105, 0.105), leaf, segments=14, rings=8)\n    parent_to_bone(tip, rig, "tail.05")\n\n    rig["character_name"] = "Oyen Purba"\n    rig["rig_version"] = "Oyen Purba 3D Rig V1"\n    rig["brand_colors"] = {\n        "blaze_orange": "#E8842A",\n        "sun_cream": "#F7D89B",\n        "deep_cocoa": "#4A2C24",\n        "amber_eyes": "#D79223",\n        "leaf_tail": "#2F7D70",\n    }\n    rig["rig_type"] = "segmented bone-parent rig"\n    return rig\n\n\ndef add_environment():\n    floor_material = make_material("Studio Floor", rgba("#E9D7B9"), 0.88)\n    backdrop_material = make_material("Studio Backdrop", rgba("#F5E7CE"), 0.92)\n    bpy.ops.mesh.primitive_plane_add(size=30, location=(2.0, 0.0, -0.04))\n    floor = bpy.context.active_object\n    floor.name = "Oyen_Studio_Floor"\n    floor.data.materials.append(floor_material)\n\n    backdrop = add_cube("Oyen_Backdrop", (2.0, 3.4, 2.4), (8.0, 0.15, 3.0), backdrop_material, bevel=0.15)\n    backdrop.rotation_euler[0] = math.radians(90)\n\n    bpy.ops.object.light_add(type="AREA", location=(2.5, -4.5, 6.2))\n    key = bpy.context.active_object\n    key.name = "Oyen_Key_Light"\n    key.data.energy = 1050\n    key.data.shape = "DISK"\n    key.data.size = 5.5\n    key.rotation_euler = (math.radians(25), 0.0, math.radians(28))\n\n    bpy.ops.object.light_add(type="AREA", location=(-3.0, -1.0, 3.8))\n    fill = bpy.context.active_object\n    fill.name = "Oyen_Fill_Light"\n    fill.data.energy = 500\n    fill.data.size = 4.5\n    fill.rotation_euler = (math.radians(65), 0.0, math.radians(-65))\n\n    bpy.ops.object.light_add(type="AREA", location=(2.0, 3.0, 4.5))\n    rim = bpy.context.active_object\n    rim.name = "Oyen_Rim_Light"\n    rim.data.energy = 700\n    rim.data.size = 3.5\n    rim.rotation_euler = (math.radians(-50), 0.0, math.radians(180))\n\n\ndef point_camera(camera, target: Vector) -> None:\n    direction = target - camera.location\n    camera.rotation_euler = direction.to_track_quat("-Z", "Y").to_euler()\n\n\ndef create_camera():\n    bpy.ops.object.camera_add(location=(6.4, -10.5, 4.3))\n    camera = bpy.context.active_object\n    camera.name = "Oyen_Camera"\n    camera.data.lens = 52\n    bpy.context.scene.camera = camera\n    point_camera(camera, Vector((0.4, 0.0, 1.25)))\n    return camera\n\n\ndef key_pose(bone, frame: int, rotation=None, location=None, scale=None) -> None:\n    if rotation is not None:\n        bone.rotation_euler = rotation\n        bone.keyframe_insert(data_path="rotation_euler", frame=frame)\n    if location is not None:\n        bone.location = location\n        bone.keyframe_insert(data_path="location", frame=frame)\n    if scale is not None:\n        bone.scale = scale\n        bone.keyframe_insert(data_path="scale", frame=frame)\n\n\ndef set_interpolation(rig, camera) -> None:\n    objects = [rig, rig.data, camera, camera.data]\n    for obj in objects:\n        animation = getattr(obj, "animation_data", None)\n        action = animation.action if animation else None\n        if action:\n            for curve in action.fcurves:\n                for point in curve.keyframe_points:\n                    point.interpolation = "BEZIER"\n    if rig.animation_data and rig.animation_data.action:\n        for curve in rig.animation_data.action.fcurves:\n            for point in curve.keyframe_points:\n                point.interpolation = "BEZIER"\n\n\ndef animate_rig(rig, camera):\n    timeline = JOB["timeline"]\n    fps = int(timeline["fps"])\n    total_frames = max(2, int(timeline["total_frames"]))\n    prompt = str(JOB.get("project", {}).get("prompt", "")).lower()\n    is_run = any(word in prompt for word in ("lari", "berlari", "run", "mengejar"))\n    is_jump = any(word in prompt for word in ("lompat", "melompat", "jump"))\n    is_surprised = any(word in prompt for word in ("terkejut", "kaget", "surprised", "piring jatuh"))\n    is_laugh = any(word in prompt for word in ("tertawa", "tersenyum", "senyum", "laugh"))\n    is_angry = any(word in prompt for word in ("marah", "geram", "angry"))\n\n    pose = rig.pose.bones\n    stride = math.radians(52 if is_run else 34)\n    travel = 4.0 if is_run else 2.8\n    samples = max(8, min(total_frames, int(timeline["duration_seconds"] * 6) + 1))\n\n    for sample in range(samples):\n        t = sample / max(1, samples - 1)\n'