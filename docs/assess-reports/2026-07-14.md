# ASSESSMENT: blender-mcp

**Date:** 2026-07-14
**Repo Type:** Standard MCP
**SOTA Score:** 72/100 (needs work — below 80 SOTA threshold)

## Score Breakdown

### Required files (14/16 = -8 pts)
- **[HIGH]** justfile: duplicate `mcpb-pack`/`cua-nsis-test` from fleet.just import — **FIXED**
- **[HIGH]** justfile: missing `fmt`, `certify` recipes — **FIXED**
- **[HIGH]** llms.txt: no link to `llms-full.txt` — **FIXED**
- **[HIGH]** `.opencode/skills/` missing — **FIXED**
- **[LOW]** `.gitignore`: missing `native/gen/` — **FIXED**
- **[MEDIUM]** `glama.json`: missing `version` field — **FIXED**
- **[LOW]** Dirty git tree (pre-existing changes) — still present

### Tool surface (6/11 = -20 pts)
- **[HIGH]** No `## Return Format` in docstrings (68 tools)
- **[HIGH]** No `## Examples` in docstrings (68 tools)
- **[HIGH]** No `annotations=` (READ_ONLY/MUTATING/DESTRUCTIVE) on any tool
- **[HIGH]** No Prefab `app=True` cards on any list/status/stats tool
- **[MEDIUM]** `# type: ignore` without error code (4 instances) — **FIXED**
- **[INFO]** Tool naming: portmanteaus use `blender_` prefix (deliberate domain choice)

### Testing (2/4 = -8 pts)
- **[HIGH]** tests exist and pass ✅
- **[MEDIUM]** No Playwright/E2E config for full coverage
- **[LOW]** No coverage threshold configured

### Webapp (4/8 = -16 pts)
- **[HIGH]** Chat: no localStorage persistence, no personalities, no export/clear
- **[HIGH]** No `/skills` frontend page (backend exposes `/api/skills`)
- **[MEDIUM]** `data-testid` missing on chat, settings, most pages
- **[MEDIUM]** `text-xs` used 97x outside `<pre>` tags
- Stack ✅, Dark theme ✅, Port config ✅, vite proxy ✅

### CORS (1/1 = -15 pts — CRITICAL)
- **[CRITICAL]** `allow_origin_regex` gated on `_blender_tauri`, missing Tailscale/LAN/CGNAT — **FIXED**
- ✅ `tauri://localhost` present, no `["*"]`, uses `http_app()` not `run_http_async()`

### Security (3/3 = 0 pts — PASS)
- ✅ build.ps1 bundles `.env.example` not `.env`
- ✅ No hardcoded secrets
- ✅ `.gitignore` covers mcpb/target

### Tauri/Native (5/7 = -8 pts)
- **[HIGH]** `backend.rs`: `free_port()` single-layer kill — **FIXED** (multi-layer + UAC + 240s poll)
- **[HIGH]** `backend.rs`: no stdout/stderr stream watching — **FIXED** (`watch_stream` thread)
- **[HIGH]** `build.ps1`: missing API_BASE gate, frozen smoke test, size gate — **FIXED**
- ✅ hooks.nsh, tauri.conf.json, Cargo.toml, useZoom, backend-status, cua-smoke all present

### Bad patterns (3/5 = -8 pts)
- **[HIGH]** `# type: ignore` without error code — **FIXED**
- **[MEDIUM]** `print()` in non-test Python (many instances — pre-existing)
- **[MEDIUM]** bare `except:` in some handlers (pre-existing)

### Error handling (2/3 = -4 pts)
- **[LOW]** `logger.exception()` vs `logger.error()` in except blocks (LOW severity)
- ✅ `_error_response` pattern used in handlers
- ✅ Module imports raise on failure

### CI/CD (4/7 = -6 pts)
- ✅ CI workflow exists with ruff, pytest steps
- Missing: `ruff format --check`, `tsc --noEmit`, `uv cache`

### Per-severity count
- CRITICAL: 1 (CORS — **FIXED**)
- HIGH: 12 (6 FIXED, 6 deferred)
- MEDIUM: 8 (3 FIXED)
- LOW: 4 (2 FIXED)

## Summary of Fixes Applied

| Severity | Issue | File |
|----------|-------|------|
| CRITICAL | CORS regex gated, missing Tailscale/LAN/CGNAT | `src/blender_mcp/server.py` |
| HIGH | justfile duplicate recipes (breaks `just --list`) | `justfile` |
| HIGH | justfile missing `fmt`, `certify` | `justfile` |
| HIGH | llms.txt no link to `llms-full.txt` | `llms.txt` |
| HIGH | `.opencode/skills/` missing | Created `.opencode/skills/blender-mcp/SKILL.md` |
| HIGH | `backend.rs` free_port single-layer, no stream watching | `native/src/backend.rs` |
| HIGH | `build.ps1` missing API_BASE gate, smoke test, size gate | `native/build.ps1` |
| MEDIUM | `glama.json` missing `version` | `glama.json` |
| MEDIUM | `.gitignore` missing `native/gen/` | `.gitignore` |
| MEDIUM | `# type: ignore` without error code (4 instances) | `agentic.py`, `transport.py`, `repository_tools.py` |

## Deferred (not fixed this session)

- Docstring SOTA (`## Return Format`, `## Examples`) across 68 tools — large refactor
- Tool annotations (READ_ONLY/MUTATING/DESTRUCTIVE) — requires 68 edits
- Prefab `app=True` cards — needs structural additions
- Chat personalities + localStorage — webapp feature work
- Skills page — new route + page component
- `text-xs` cleanup (97 instances) — design audit
- `data-testid` coverage — incrementally fix per page
